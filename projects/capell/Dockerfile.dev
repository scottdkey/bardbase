FROM golang:alpine

RUN go install github.com/air-verse/air@latest

WORKDIR /app

# Pre-cache modules so the first hot-reload build is fast.
# The go-pkg-cache volume persists this cache between container restarts.
COPY go.mod go.sum ./
RUN go mod download

CMD ["air"]
