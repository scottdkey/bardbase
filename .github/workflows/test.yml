name: Test

on:
  push:
    branches: [main]
    paths:
      - 'projects/capell/**'
      - '.github/workflows/**'
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/capell
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: projects/capell/go.mod
      - name: Run tests
        run: go test ./... -v
